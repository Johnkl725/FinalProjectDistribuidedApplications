# ===============================================
# ENVIRONMENT VARIABLES TEMPLATE
# Copy this file to .env and fill with your values
# ===============================================

# Database Configuration
DB_NAME=insurance_db
DB_USER=postgres
DB_PASSWORD=your-secure-password-here

# JWT Configuration
# IMPORTANT: Generate a strong secret (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
JWT_EXPIRES_IN=24h

# JWT Refresh Token Configuration (Optional - for token renewal)
# Uncomment these lines if you implement refresh tokens
REFRESH_TOKEN_SECRET=your-refresh-token-secret-min-32-chars
REFRESH_TOKEN_EXPIRES_IN=7d  # Refresh tokens last 7 days

# Rate Limiting Configuration
# Window: 15 minutes (900000ms)
# Max Requests: 100 per window
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===============================================
# PRODUCTION NOTES:
# 1. NEVER commit .env to git
# 2. Use strong passwords (min 16 characters)
# 3. Generate JWT secret with: openssl rand -base64 32
# 4. Consider using environment-specific files:
#    - .env.development
#    - .env.staging
#    - .env.production
# ===============================================
