# ===============================================
# PRODUCTION ENVIRONMENT VARIABLES - VPS DEPLOYMENT
# Server IP: 178.128.70.171
# ===============================================

# Database Configuration
# IMPORTANT: Update these with your actual database credentials
# If using Docker Compose postgres service, use: postgres
# If using external database, use the external host
DB_HOST=postgres
DB_PORT=5432
DB_NAME=insurance_db
DB_USER=postgres
DB_PASSWORD=postgres_secure_password_2024

# JWT Configuration
# CRITICAL: Generate a secure secret with: openssl rand -base64 32
JWT_SECRET=YourSecureJWTSecretKeyMinimum32CharactersLong2024
JWT_EXPIRES_IN=24h

# Refresh Token Configuration (Optional)
REFRESH_TOKEN_SECRET=YourSecureRefreshTokenSecretMin32Chars2024
REFRESH_TOKEN_EXPIRES_IN=7d

# Rate Limiting Configuration
# 15 minutes window, 100 requests max
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# VPS Configuration
# This is your public IP where the application will be accessible
VPS_IP=178.128.70.171
API_GATEWAY_URL=http://178.128.70.171:3000

# Frontend API URL (used by Vite at build time)
VITE_API_URL=http://178.128.70.171:3000/api

# ===============================================
# SECURITY NOTES:
# 1. Change DB_PASSWORD to a strong password
# 2. Generate JWT_SECRET with: openssl rand -base64 32
# 3. This is a template - copy and modify for your deployment
# 4. For production, consider using secrets management
# ===============================================
